# 🌱 Plant Disease Detector

AI-powered веб-приложение для определения болезней растений с использованием нейросети ResNet50.

## 🚀 Быстрый старт

### 1. Клонирование репозитория
\`\`\`bash
git clone https://github.com/romaplaton1995-alt/plant-disease-detector2.git
cd plant-disease-detector
\`\`\`

### 2. Скачивание больших файлов (модели и зависимости)

**Важно:** Из-за ограничений GitHub (максимальный размер файла 100MB), большие файлы моделей и зависимостей необходимо скачать отдельно.

#### 2.1 Скачивание предобученной ResNet50
```bash
mkdir -p models/resnet50_weights
wget https://download.pytorch.org/models/resnet50-11ad3fa6.pth -O models/resnet50_weights/resnet50-11ad3fa6.pth

#### 2.2 Скачивание обученной модели
# Скачайте вашу обученную модель (plant_disease_resnet50_best.pth)
# и поместите её в папку models/
mkdir -p models
# Скачайте файл из вашего надежного источника
# wget https://your-storage-url/plant_disease_resnet50_best.pth -O models/plant_disease_resnet50_best.pth

# 2.3 Скачивание локальных зависимостей PyTorch
# Скачайте следующие файлы:
wget https://download.pytorch.org/whl/cu118/torch-2.7.1%2Bcu118-cp312-cp312-linux_x86_64.whl
wget https://download.pytorch.org/whl/cu118/torchvision-0.22.1%2Bcu118-cp312-cp312-linux_x86_64.whl  
wget https://download.pytorch.org/whl/cu118/torchaudio-2.7.1%2Bcu118-cp312-cp312-linux_x86_64.whl

# 2.4 Установка локальных зависимостей
pip install torch-2.7.1+cu118-cp312-cp312-linux_x86_64.whl
pip install torchvision-0.22.1+cu118-cp312-cp312-linux_x86_64.whl
pip install torchaudio-2.7.1+cu118-cp312-cp312-linux_x86_64.whl

# 3. Сборка и запуск Docker контейнера
```bash
docker-compose build
docker-compose up -d
```
# 4. Доступ к приложению
Откройте браузер по адресу:
```
http://localhost:5000
```
# 5. Остановка приложения
```bash
docker-compose down
```
📦 Требования
Docker Engine версии 20.10+
Docker Compose версии 2.0+
Git для клонирования репозитория
Минимум 4GB оперативной памяти
Минимум 2 ядра процессора
Минимум 2GB свободного места на диске
NVIDIA GPU с поддержкой CUDA 11.8 (рекомендуется для лучшей производительности)
🌿 Поддерживаемые растения и болезни
🌶️ Перец (Pepper)
Бактериальное пятно (Bacterial spot)
Здоровое растение (Healthy)
🥔 Картофель (Potato)
Ранняя фитофтора (Early blight)
Поздняя фитофтора (Late blight)
Здоровое растение (Healthy)
🍅 Помидоры (Tomato)
Бактериальное пятно (Bacterial spot)
Ранняя фитофтора (Early blight)
Поздняя фитофтора (Late blight)
Плесень листьев (Leaf Mold)
Пятнистость листьев (Septoria leaf spot)
Паутинный клещ (Spider mites)
Целевое пятно (Target Spot)
Вирус курчавости желтых листьев томата (Tomato Yellow Leaf Curl Virus)
Вирус томатной мозаики (Tomato mosaic virus)
Здоровое растение (Healthy)
📂 Структура проекта
```
plant-disease-detector/
├── app/ # Flask приложение
│ ├── init.py # Фабрика приложения
│ ├── routes.py # Маршруты
│ ├── model_loader.py # Загрузка и инференс модели
│ ├── templates/ # HTML шаблоны
│ └── static/ # Статические файлы (CSS, JS, изображения)
├── models/ # Модели машинного обучения
│ └── resnet50_weights/ # Веса предобученной ResNet50
├── Dockerfile # Инструкции для сборки Docker образа
├── docker-compose.yml # Конфигурация Docker Compose
├── .dockerignore # Файлы для игнорирования при сборке
├── .gitignore # Файлы для игнорирования Git
├── requirements.txt # Python зависимости
├── run.py # Точка входа приложения
├── LICENSE # Лицензия MIT
└── README.md # Документация
```

Volume монтирование
./app/static/uploads: Директория для сохранения загруженных изображений (сохраняется между перезапусками контейнера)

Поддержка GPU
Приложение поддерживает GPU ускорение через CUDA 11.8. Для использования GPU:

Установите NVIDIA Container Toolkit
Добавьте в docker-compose.yml:
    services:
  web:
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all


